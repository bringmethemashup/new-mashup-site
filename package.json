{
  "name": "bring-me-the-mashup",
  "version": "2.0.0",
  "private": true,
  "description": "Bring Me The Mashup — static site + Capacitor Android shell. The Android APK is built by .github/workflows/build-apk.yml; the site itself has no build step.",
  "scripts": {
    "start": "python3 -m http.server 8080"
  }
}
